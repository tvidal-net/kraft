rootProject.name = 'kraft'

include ':cli'
include ':client'
include ':codec'
include ':core'
include ':server'
include ':storage'
include ':test'
include ':transport'

rootProject.children.each { it.name = "kraft-${it.name}" }
