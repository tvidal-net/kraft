rootProject.name = 'kraft'

include ':cli'
include ':client'
include ':codec'
include ':core'
include ':server'
include ':transport'
include ':local'

rootProject.children.each { it.name = "kraft-${it.name}" }
